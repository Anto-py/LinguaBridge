// config.js — Seul fichier à modifier après déploiement Vercel
// Chargé avant content.js dans manifest.json

const LB_CONFIG = {
  // ⚠️  Remplacer par l'URL réelle après `vercel deploy`
  PROXY_URL: 'https://YOUR_PROJECT.vercel.app/api/claude',
};
